specVersion: "v1"
serviceId: "myproject-63a54-service"
location: "us-east4"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "myproject-63a54-database"
        cloudSql:
          instanceId: "myproject-63a54-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./tasks-connector", "./auth-connector"]
